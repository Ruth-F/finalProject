import { MinyanimDirective } from './minyanim.directive';

describe('MinyanimDirective', () => {
  it('should create an instance', () => {
    const directive = new MinyanimDirective();
    expect(directive).toBeTruthy();
  });
});
